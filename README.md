# linux-server-OS-upgrade

**cat /etc/os-release

uname -r(optional)

dpkg -l(package installations)

tar -czf etc_backup.tar.gz /etc

apt update

apt upgrade -y

do-release-upgrade

cat /etc/os-release**
